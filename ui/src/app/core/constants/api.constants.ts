export const API = {
  AUTH: '/auth',
  PROFILE: '/profile',
  RESUME_VERSIONS: '/resume-versions'
} as const;


export const CONSTANTS = {
  beta: 'BETA (Expect Delay in response, hosted in free tire)'
} 